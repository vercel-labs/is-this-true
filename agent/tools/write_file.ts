// Disabled: this template fact-checks using ONLY the X MCP (agent/connections/x.ts),
// so the default harness tool "write_file" is turned off. Delete this file to re-enable it.
import { disableTool } from "eve/tools";

export default disableTool();
